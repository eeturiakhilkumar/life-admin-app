import { ModuleScreen } from "../../src/features/module-screen";

export default function ShoppingListsRoute() {
  return (
    <ModuleScreen
      type="shopping_list"
      title="Shopping Lists"
      subtitle="Capture repeatable errands, groceries, and prep lists from the same quick-add flow."
    />
  );
}

